{
  "manifest_version": "0.4",
  "name": "obs-mcp",
  "version": "1.1.1",
  "display_name": "OBS MCP",
  "description": "Control OBS Studio through 158 current, annotated MCP tools",
  "long_description": "OBS MCP connects compatible MCP clients to OBS Studio through the built-in WebSocket v5 server. It covers the synchronized OBS request surface for scenes, sources, audio, filters, transitions, recording, streaming, canvases, and configuration. Tool discovery remains available while OBS is offline, and the server reconnects in the background.\n\nRequirements:\n- Node.js 20.19 or newer\n- OBS Studio 28 or newer with WebSocket Server enabled under Tools > WebSocket Server Settings",
  "icon": "icon.png",
  "author": {
    "name": "Thomas Main",
    "url": "https://github.com/Tom-R-Main"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Tom-R-Main/OBS-MCP"
  },
  "homepage": "https://github.com/Tom-R-Main/OBS-MCP",
  "documentation": "https://github.com/Tom-R-Main/OBS-MCP#readme",
  "support": "https://github.com/Tom-R-Main/OBS-MCP/issues",
  "license": "GPL-2.0-only",
  "tools_generated": true,
  "keywords": [
    "obs",
    "streaming",
    "recording",
    "video",
    "broadcast",
    "scenes"
  ],
  "compatibility": {
    "platforms": [
      "darwin",
      "win32",
      "linux"
    ],
    "runtimes": {
      "node": ">=20.19.0"
    }
  },
  "server": {
    "type": "node",
    "entry_point": "server/index.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/server/index.js"
      ],
      "env": {
        "OBS_WEBSOCKET_URL": "${user_config.obs_websocket_url}",
        "OBS_WEBSOCKET_PASSWORD": "${user_config.obs_websocket_password}",
        "OBS_MCP_TOOLSETS": "${user_config.obs_mcp_toolsets}",
        "OBS_MCP_READ_ONLY": "${user_config.obs_mcp_read_only}",
        "OBS_MCP_CONFIRM_LIVE": "${user_config.obs_mcp_confirm_live}",
        "OBS_MCP_READ_OBS_CONFIG": "${user_config.obs_mcp_read_obs_config}"
      }
    }
  },
  "user_config": {
    "obs_websocket_url": {
      "type": "string",
      "title": "OBS WebSocket URL",
      "description": "WebSocket URL for OBS Studio. Configure in OBS: Tools → WebSocket Server Settings",
      "required": false,
      "default": "ws://localhost:4455"
    },
    "obs_websocket_password": {
      "type": "string",
      "title": "OBS WebSocket Password",
      "description": "Password from OBS WebSocket Server Settings. Leave empty if authentication is disabled.",
      "required": false,
      "sensitive": true
    },
    "obs_mcp_toolsets": {
      "type": "string",
      "title": "Tool groups",
      "description": "Comma-separated tool groups to expose: all, core, or any of general, scenes, scene-items, sources, inputs, media, filters, transitions, record, stream, outputs, config, ui, protocol.",
      "required": false,
      "default": "all"
    },
    "obs_mcp_read_only": {
      "type": "boolean",
      "title": "Read-only mode",
      "description": "Expose only tools that do not change OBS.",
      "required": false,
      "default": false
    },
    "obs_mcp_confirm_live": {
      "type": "boolean",
      "title": "Confirm live actions",
      "description": "Ask before stopping or toggling a stream or recording.",
      "required": false,
      "default": false
    },
    "obs_mcp_read_obs_config": {
      "type": "boolean",
      "title": "Use the password saved in OBS",
      "description": "When no password is entered above, read the one OBS saved on this computer.",
      "required": false,
      "default": false
    }
  }
}
